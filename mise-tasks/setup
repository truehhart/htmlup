#!/usr/bin/env nu
# Download Go module dependencies and install pre-commit hooks.

print "→ downloading Go modules"
go mod download

print "→ installing pre-commit hooks"
pre-commit install

print "✓ setup complete — `mise run check` runs fmt + vet + lint + test"
